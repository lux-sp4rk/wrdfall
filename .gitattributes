# No LFS tracking — dist/ files are build output from landing/public/ via Vite.
# Godot export binaries live in landing/public/ (committed directly, no LFS).
# See: https://github.com/lux-sp4rk/word-loom/issues/171
