name: Bug Hunter D33

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  bug-hunter-d33:
    name: Bug Hunter D33
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Summon the Hunter
        uses: lux-sp4rk/bug-hunter-d33@55c5e2ca2f1d94823925b3e8ca7b1bc217f1fe75
        continue-on-error: true
        with:
          arcee-api-key: ${{ secrets.ARCEE_API_KEY }}
          model: 'arcee/trinity-mini'
          framework: godot
          passes: 'logic,security,judge,performance'
          severity-threshold: 'note'
          max-files: 50
          custom-prompts-path: '.ai-memory'
